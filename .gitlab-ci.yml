stages:
  - build

.pages-build: &build
  image: ghcr.io/astral-sh/uv:latest
  script:
    - ./build.bash
  artifacts:
    paths:
      - public

test-pages:
  stage: build
  <<: *build
  except:
    - main

pages:
  stage: build
  <<: *build
  only:
    - main
